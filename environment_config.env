# Expert Company LLM Assistant - Environment Configuration
# Following Context7 FastAPI best practices

# Environment settings (affects documentation visibility)
# Options: local, development, staging, production
ENVIRONMENT=local

# Documentation URLs (automatically configured based on ENVIRONMENT)
# In production, these will be set to None for security
SHOW_DOCS_ENVIRONMENT=local,development,staging

# API Configuration
API_VERSION=1.0.0
API_TITLE=Expert Company LLM Assistant
API_DESCRIPTION=Multilingual AI assistant for petroleum and training services

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:8000,http://127.0.0.1:3000,http://127.0.0.1:8000
CORS_ALLOW_CREDENTIALS=true

# Server Configuration
HOST=127.0.0.1
PORT=8001
WORKERS=1

# Logging Configuration
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Request Configuration
REQUEST_TIMEOUT=30
MAX_REQUEST_SIZE=10485760

# Optional: External Service URLs
EXPERT_COMPANY_DOCS_URL=https://expertcompany.com/docs
EXPERT_COMPANY_SUPPORT_EMAIL=support@expertcompany.com 